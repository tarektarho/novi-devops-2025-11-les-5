# Startpunt CI Workflow
# TODO: Breid uit naar multi-job workflow met Docker build & push

name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

      - name: Run tests
        run: npm test

  # TODO: Voeg build job toe
  # build:
  #   needs: test
  #   runs-on: ubuntu-latest
  #   permissions:
  #     packages: write
  #   steps:
  #     - Checkout
  #     - Login to GHCR
  #     - Build Docker image
  #     - Push to GHCR
